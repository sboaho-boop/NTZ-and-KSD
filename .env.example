# Postgres connection string (create a free project at neon.tech)
DATABASE_URL="postgresql://user:password@host/dbname?sslmode=require"

# Secret used to sign the admin session cookie (32+ hex chars, e.g. openssl rand -hex 32)
ADMIN_SECRET=""